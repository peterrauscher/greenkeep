/**
 * GitHub is the only sign-in method. The write path needs a GitHub access
 * token with `repo` + `user:email`, stored on the linked Better Auth account.
 */
export const GITHUB_PROVIDER_ID = "github";
